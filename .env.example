# docker compose variables (copy to .env; .env is git-ignored)
API_KEY=change-me-long-random            # required by the BFF; injected by nginx, never exposed to the browser
RATE_LIMIT_PER_MINUTE=120
# Live model (optional; default is the dummy catalogue). Any OpenAI-compatible endpoint.
# AI_PROVIDER=openai_compatible
# AI_BASE_URL=https://api.deepseek.com
# AI_MODEL=deepseek-chat
# AI_API_KEY=sk-...
